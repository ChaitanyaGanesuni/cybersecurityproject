{
  "name": "@gita/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API: holds LLM/TTS keys, serves grounded AI + content + TTS-proxy endpoints to the app.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gita/ai": "*",
    "@gita/rag": "*",
    "@gita/core": "*",
    "@gita/content": "*",
    "@gita/contracts": "*",
    "fastify": "^5.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
